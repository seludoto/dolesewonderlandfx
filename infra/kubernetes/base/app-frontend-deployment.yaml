apiVersion: apps/v1
kind: Deployment
metadata:
  name: dolesewonderlandfx-app-frontend
  labels:
    app: dolesewonderlandfx-app-frontend
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dolesewonderlandfx-app-frontend
  template:
    metadata:
      labels:
        app: dolesewonderlandfx-app-frontend
        component: frontend
    spec:
      containers:
      - name: app-frontend
        image: dolesewonderlandfx/app-frontend:latest
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: API_URL
          value: "http://dolesewonderlandfx-api:8000"
        - name: AUTH_API_URL
          value: "http://dolesewonderlandfx-auth:8002"
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      imagePullSecrets:
      - name: ecr-secret