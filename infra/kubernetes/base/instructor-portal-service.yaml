apiVersion: v1
kind: Service
metadata:
  name: dolesewonderlandfx-instructor-portal
  labels:
    app: dolesewonderlandfx-instructor-portal
    component: frontend
spec:
  selector:
    app: dolesewonderlandfx-instructor-portal
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP